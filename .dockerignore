# Build artifacts
dist/
build/
docs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Dependencies (在镜像里重装)
node_modules/
.pnpm-store/

# Testing
coverage/
.nyc_output/

# IDE
.vscode/
.idea/
.history/
.DS_Store

# Git
.git/
.gitignore
.gitattributes

# CI
.github/

# Docker
# 只排除开发用的 Dockerfile (多阶段全栈) 和 docker-compose.yml (本地全栈)
# 不要排除 Dockerfile.prod / docker-compose.prod.yml (服务器专用, build 需要)
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.override.yml

# Env files (不要 commit secrets 进镜像)
.env
.env.local
.env.*.local

# README / License (可选, 节省空间)
README.md
LICENSE

# Tests
test/
__tests__/
*.spec.ts
*.test.ts